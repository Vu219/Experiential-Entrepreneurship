// Đã chuyển thành component dùng chung src/components/ConfirmModal.tsx (đợt đồng bộ UI admin).
// Giữ file re-export để các import sẵn có (brand/create/admin) không phải sửa hàng loạt.
export { default } from '../ConfirmModal';
